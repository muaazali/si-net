namespace SiNet
{
    public static class ConfigDefaults
    {
        public const int MAX_CLIENTS = 8;
        public const bool SHOULD_PING_CLIENTS = true;
        public const float PING_INTERVAL = 5.0f;
        public const int MAX_PACKET_SIZE = 4096;
    }
}